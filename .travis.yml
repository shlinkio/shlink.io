language: node_js

node_js:
  - "14.15.0"

cache:
  directories:
    - node_modules

services:
  - docker

install:
  - npm ci

script:
  - npm run lint
  - npm run test
  - if [[ $TRAVIS_PULL_REQUEST != 'false' ]]; then docker build -t shlink-website:test . ; fi
